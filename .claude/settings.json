{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|resume",
        "hooks": [
          {
            "type": "command",
            "command": "if [ \"$CLAUDE_CODE_REMOTE\" = true ] && [ -n \"$AWS_ACCESS_KEY_ID\" ]; then cd \"$CLAUDE_PROJECT_DIR\" && uv run --no-sync python scripts/loop_env.py; fi"
          }
        ]
      }
    ]
  }
}
