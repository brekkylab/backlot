services:
  mock:
    build: .
    image: enterprise-mock:latest
    ports:
      - "8000:8000"
    # Override the admin/service token if desired:
    # environment:
    #   MOCK_ADMIN_TOKEN: my-service-token
