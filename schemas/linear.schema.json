{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://brekkylab.dev/schemas/linear.schema.json",
  "title": "BYO corpus record — linear",
  "description": "A Linear issue. `team` is the grouping unit, because `data.team.issues` is how both the GraphQL API and the official clients reach issues and because an issue's identifier prefix (ENG-123) IS the team key. Field names are Linear's own — `state` (not status), `estimate`, camelCase `branchName`/`dueDate` — so a corpus written against the real API needs no renaming. `identifier` and `branchName` are derived when omitted. Comments are served through the `Issue.comments` connection.",
  "type": "object",
  "properties": {
    "source_type": {
      "const": "linear"
    },
    "title": {
      "type": "string",
      "minLength": 1,
      "description": "The issue title."
    },
    "content": {
      "type": "string",
      "minLength": 1,
      "description": "The issue's markdown description — served as `Issue.description` and indexed for search."
    },
    "doc_id": {
      "type": "string",
      "description": "Stable id (default: dsid_<sha256(source_type+title+content)>). The served `Issue.id` UUID is derived from it."
    },
    "team": {
      "type": "string",
      "description": "The owning team, e.g. \"engineering\". Its short key (ENG) is derived from the name and is what identifiers are prefixed with. Default: the source_type."
    },
    "group": {
      "type": "string",
      "description": "ACL group owning the team (default: slug(team))."
    },
    "identifier": {
      "type": "string",
      "description": "Human identifier, e.g. \"ENG-123\". Default: synthesized as <TEAMKEY>-<n> from the doc_id. Not required to be unique — the bench's own keys repeat — and `issue(id:)` resolves a repeated identifier to the first matching issue."
    },
    "state": {
      "type": "string",
      "description": "Workflow state NAME, e.g. \"In Progress\" / \"Done\" / \"Backlog\". Its Linear category (triage|backlog|unstarted|started|completed|canceled) is derived from the name and served as `state.type`."
    },
    "priority": {
      "type": [
        "integer",
        "string"
      ],
      "description": "Linear's 0-4 scale (0 none, 1 urgent, 2 high, 3 medium, 4 low). A label (\"Urgent\", \"High\") or a bench-style \"P0\"-\"P3\" is accepted and normalized onto that scale."
    },
    "estimate": {
      "type": [
        "integer",
        "number",
        "null"
      ],
      "description": "Complexity estimate in the team's scale."
    },
    "labels": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Label names, served through the `labels` connection."
    },
    "project": {
      "type": "string",
      "description": "Project name; served as `project { name }`."
    },
    "cycle": {
      "type": "string",
      "description": "Cycle name, e.g. \"2025-W08\"; served as `cycle { name }`."
    },
    "branchName": {
      "type": "string",
      "description": "Suggested git branch. Default: Linear's own derivation, <assignee>/<identifier>-<title slug>."
    },
    "dueDate": {
      "type": "string",
      "description": "Due date as a bare YYYY-MM-DD (Linear's TimelessDate scalar)."
    },
    "assignee": {
      "type": "string",
      "format": "email",
      "description": "Assignee's email. Omit for an unassigned issue — `assignee` then resolves to null, as it does in Linear."
    },
    "assigneeName": {
      "type": "string",
      "description": "Assignee display name (default: derived from the email)."
    },
    "author_email": {
      "type": "string",
      "format": "email",
      "description": "The issue creator; served as `creator`."
    },
    "author_name": {
      "type": "string"
    },
    "author_groups": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Groups the creator belongs to."
    },
    "visibility": {
      "enum": [
        "public",
        "group",
        "private"
      ],
      "description": "public = org-wide, group = the team's group, private = creator only."
    },
    "readers": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Explicit reader principals (emails -> users, else group ids). Overrides visibility."
    },
    "comments": {
      "type": "array",
      "description": "Comments on the issue, served through `Issue.comments`.",
      "items": {
        "type": "object",
        "properties": {
          "content": {
            "type": "string"
          },
          "body": {
            "type": "string"
          },
          "id": {
            "type": "string"
          },
          "author_email": {
            "type": "string",
            "format": "email"
          },
          "author_name": {
            "type": "string"
          },
          "created_ts": {
            "type": [
              "integer",
              "number",
              "string"
            ]
          },
          "reactions": {
            "type": "array",
            "items": {
              "type": "object"
            }
          }
        },
        "additionalProperties": false
      }
    },
    "meta": {
      "type": "object",
      "description": "Free-form per-source structured extras merged into the record."
    },
    "created": {
      "type": [
        "integer",
        "number",
        "string"
      ],
      "description": "createdAt (epoch seconds or ISO 8601). Default: synthesized from doc_id."
    },
    "updated": {
      "type": [
        "integer",
        "number",
        "string"
      ],
      "description": "updatedAt (epoch seconds or ISO 8601). Defaults to createdAt when absent, since Linear declares updatedAt non-null."
    },
    "archivedAt": {
      "type": [
        "integer",
        "number",
        "string"
      ]
    },
    "autoArchivedAt": {
      "type": [
        "integer",
        "number",
        "string"
      ]
    },
    "autoClosedAt": {
      "type": [
        "integer",
        "number",
        "string"
      ]
    },
    "canceledAt": {
      "type": [
        "integer",
        "number",
        "string"
      ]
    },
    "completedAt": {
      "type": [
        "integer",
        "number",
        "string"
      ]
    },
    "startedAt": {
      "type": [
        "integer",
        "number",
        "string"
      ]
    }
  },
  "required": [
    "source_type",
    "content",
    "title"
  ],
  "additionalProperties": false
}
